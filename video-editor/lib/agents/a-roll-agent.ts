import { BaseSpecializedAgent } from "./base-specialized-agent";

export class ARollAgent extends BaseSpecializedAgent {
  name = "A-Roll Specialist";
  role = "Handles talking-head visual generation via HeyGen simulation (Lip-sync, Voice Cloning).";
}
